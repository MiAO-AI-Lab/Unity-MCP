{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Unity MCP Server Launch",
            "type": "coreclr",
            "request": "launch",
            "preLaunchTask": "build",
            "program": "${workspaceFolder}/bin~/Debug/net9.0/com.MiAO.MCP.Server.dll",
            "args": [],
            "cwd": "${workspaceFolder}",
            "stopAtEntry": false,
            "console": "integratedTerminal"
        }
    ]
}